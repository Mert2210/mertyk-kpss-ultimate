{
  "name": "mertyk-kpss-ultimate-fullstack",
  "version": "1.0.0",
  "description": "Fullstack deployment for Azure App Service",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd server && npm install"
  }
}
